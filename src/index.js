import $ from "jquery";
import _ from "lodash";
import html from './indicatorBuilder.html';
import './indicatorBuilder.scss';

export const init = () => {
   console.warn(html);
};

export default { init };
